<runtime>
    <archive useRoot="false" name="${project.artifactId}-${project.version}.zip"/>

    <fileSet to="/terraform-resources/0.12">
      <directory path="${basedir}/0.12"/>
    </fileSet>

    <fileSet to="/forms">
      <directory path="${basedir}/src/main/concord/forms"/>
    </fileSet>

</runtime>
